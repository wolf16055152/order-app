# 서버 설정
PORT=3001

# 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_NAME=coffee_order_db
DB_USER=postgres
DB_PASSWORD=your_password
# Render 사용 시 개별 값 대신 DATABASE_URL만 사용 가능
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# 운영 환경(Render) 권장
DB_SSL=false

# 환경 설정
NODE_ENV=development